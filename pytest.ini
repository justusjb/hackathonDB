[pytest]
# Automatically set this environment variable when pytest runs
env =
    PYTEST_RUNNING=1
# Optional: Specify where tests are located
testpaths = tests
# Optional: Add default command line options
# addopts = -v -s